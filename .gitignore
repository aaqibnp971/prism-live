# ---------------------------------------------------------------------------
# Prism Live
# ---------------------------------------------------------------------------

# --- Python ---------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
.venv/
venv/
env/
ENV/
.python-version

# Packaging / dependency tooling
pip-log.txt
pip-delete-this-directory.txt
.pdm.toml
__pypackages__/

# Test, coverage, type and lint caches
.pytest_cache/
.tox/
.nox/
.cache/
htmlcov/
.coverage
.coverage.*
coverage.xml
*.cover
nosetests.xml
.hypothesis/
.mypy_cache/
.dmypy.json
dmypy.json
.pyre/
.pytype/
.ruff_cache/

# Notebooks
.ipynb_checkpoints/

# --- Engine source --------------------------------------------------------
# A fresh clone of upstream prism-core at acbfd50, built into a library here.
# The engine is consumed through its C ABI and never committed into this repo
# (CLAUDE.md hard rule 1).
vendor/prism-core/

# --- Logs and session captures --------------------------------------------
# Physiology is LOCAL ONLY, including derived reports and listening renders.
# These rules apply at every depth, including booth session logs.
private-data/
captures/
capture/
recordings/
recording/
session-logs/
session_logs/
sessions/
*-capture-*/
*-recording-*/
polar-mqtt-*/
verity-probe-*/
logs/
*.log
*.jsonl
raw.json
*.edf
*.bdf
*.fit
*.tcx
# Only this deterministic, generated synthetic golden fixture is publishable.
!tools/fixtures/synthetic-clean.jsonl
tools/fixtures/*
!tools/fixtures/README.md
!tools/fixtures/synthetic-clean.jsonl

# --- Local configuration and secrets --------------------------------------
.env
.env.*
!.env.example
*.local.toml
local_settings.py

# --- Audio working files --------------------------------------------------
# The four delivered stems live in assets/ and ARE tracked. Everything below
# is intermediate: DAW sessions, bounces, scratch renders and waveform caches.
# Prompt 2.7's deterministic placeholders are regenerated by
# `python -m tools.make_placeholder_stems`; the composer's delivered files are not put here.
assets/placeholders/*.wav
#
# Convention: put anything you are still working on under a working/ or _wip/
# folder and it stays out of the repo.
working/
_wip/
bounces/
renders/
scratch/
assets/**/working/
assets/**/_wip/

# DAW project and session files
*.als
*.alp
*.flp
*.rpp
*.rpp-bak
*.logicx
*.ptx
*.ptf
*.aup
*.aup3
*.band
*.cpr
*.npr
*.song
*.sesx
*.reason
*.RPP

# Waveform / peak / analysis caches
*.asd
*.reapeaks
*.sfk
*.pkf
*.peak
*.otio
*.aup3-wal
*.aup3-shm

# Uncompressed intermediates and raw captures. Delivered stems are added
# explicitly with `git add -f assets/<name>.wav` if they match these.
*.rf64
*.w64
*.caf
*.pcm
*.raw
*_render.wav
*_bounce.wav
*_scratch.wav
*-take[0-9]*.wav

# --- Editors and OS -------------------------------------------------------
.vscode/
!.vscode/extensions.json
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db
ehthumbs.db
desktop.ini
$RECYCLE.BIN/

# --- Unity (stretch goal; unity/ does not exist yet) ----------------------
unity/[Ll]ibrary/
unity/[Tt]emp/
unity/[Oo]bj/
unity/[Bb]uild/
unity/[Bb]uilds/
unity/[Ll]ogs/
unity/[Uu]ser[Ss]ettings/
unity/[Mm]emoryCaptures/
*.pidb.meta
*.pdb.meta
*.mdb.meta
